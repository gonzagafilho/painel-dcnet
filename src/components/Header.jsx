export default function Header() {
  return (
    <header style={{
      height: '60px',
      background: 'var(--bg-card)',
      display: 'flex',
      alignItems: 'center',
      padding: '0 20px'
    }}>
      <strong>Painel Administrativo</strong>
    </header>
  )
}
